# MVP 审计：这个闭环证明了什么

**审计日期：2026-09-04**

## 结论

本项目没有白做，但当前实现只证明了“软件管线可以串通”，没有证明“AI 能从真实现场资料低成本地产生可信数字孪生”。

当前已成立的闭环是：

```text
人工编写/规则生成的空间 JSON
→ compiler 与极简 IR
→ Blender primitive 场景
→ GLB 与资产清单
→ Web 三维浏览、对象点击
→ 模拟遥测和告警显示
```

白皮书真正需要验证的闭环仍然是：

```text
真实平面图、照片、设备清单和已知尺寸
→ AI 提取带证据和不确定性的空间语义
→ 人工校准并保留修改
→ 与真实资产和一个授权数据源绑定
→ 发生现场变化后增量更新
→ 与人工基线比较全周期成本
```

因此，当前成果是一个有用的 **管线原型（pipeline prototype）**，还不能称为已经验证的低成本数字孪生 MVP。

## 与白皮书的逐项核对

| 白皮书命题 | 当前证据 | 审计判断 |
| --- | --- | --- |
| 空间语义与 Blender 解耦 | JSON、compiler、IR、executor 已存在 | 已验证 |
| 使用稳定对象标识 | Blender 对象写入 `digital_twin_id`，GLB 保留 extras | 已验证 |
| 场景进入 Web 并可交互 | GLB、Three.js、点选和详情面板可运行 | 已验证 |
| 状态映射与实时更新 | 51 个资产通过 WebSocket 显示随机指标 | 仅验证 UI/传输管线；未验证真实设备接入 |
| AI 从照片理解空间 | `ai_input.py` 仍是空接口 | 未验证 |
| 多角度输入提升真实性 | `refine_from_multiview.py` 用固定规则增加对象，并直接改写置信度 | 未验证；这是人工编码的合成演示 |
| 标准组件库降低重复建模 | 当前主要把几何展开成约 350 个 primitive 对象 | 未验证；尚无真正的参数化组件版本与复用统计 |
| 人工校准建立可信映射 | 尚无校准界面、待确认项或确认记录 | 未验证 |
| 保留人工修改并做增量更新 | 每次重建托管 Collection | 未验证 |
| 数据过期显示 stale/unknown | 服务端持续生成随机时间戳 | 未实现 |
| 降低建设与维护总成本 | 没有人工基线、工时和第二现场数据 | 未验证 |

这与白皮书并不矛盾。白皮书已经明确把模拟数据、无人照片重建和未经测量的降本比例排除在有效证据之外。需要修正的是对现有演示的称呼和后续验证纪律，而不是推翻白皮书。

## “ChatGPT 可以全部完成”意味着什么

ChatGPT 能够在一次会话中生成 JSON、Blender 脚本、网页和模拟接口。这说明：

1. 通用编码与一次性场景搭建正在快速商品化。
2. “AI 生成一个可点击的三维网页”不能作为产品壁垒。
3. 这也支持白皮书的基础判断：AI 确实可能减少一部分重复理解、建模和配置劳动。

但本次过程仍由用户持续给出目标、检查画面、指出材质和视角问题，并接受未经现场核对的尺寸与布局。它更接近“ChatGPT 充当一次性的实施工程师”，不是普通用户上传资料后可以可靠复现的自动系统。

如果项目只做以下能力，应考虑停止独立产品化：

- 照片或文字生成一次性 Blender 场景；
- 把 GLB 放到通用三维网页中；
- 点击模型后显示一组预先定义或模拟的指标；
- 把“由 ChatGPT 写完代码”当作核心优势。

仍可能形成价值的部分是 ChatGPT 默认不拥有、也不会替项目长期负责的现场状态：

- 可执行的拍摄、尺度标定与验收规范；
- 带版本、证据和不确定性的空间语义；
- 经过验证的垂直组件库及参数规则；
- 现实资产、数字对象与数据点位的可追溯映射；
- 面向真实 CMDB、DCIM 或监控系统的适配器；
- 权限、审计、离线部署、数据过期与故障语义；
- 人工修正保留、差异审阅与增量更新；
- 第二、第三现场的复用数据和真实成本记录。

模型和渲染器都应被视为可替换供应件。项目的长期资产应该是经过验证的数据、组件、连接器、校准历史和交付流程。

## 代码与实现风险

1. **Schema 与 compiler 不一致。** compiler 接受 `parent_id`、`metadata` 和 `material`，JSON Schema 没有正式定义它们；Schema 也没有禁止未知字段，因而不能成为严格接口契约。
2. **“多视角置信度”没有证据链。** 脚本会把低于 0.8 的置信度直接改成 0.92，且把同一组视角赋给对象，没有视觉推理、视角级检测结果或几何一致性检查。
3. **不是组件库，而是展开后的对象清单。** L4 场景约 350 个对象、JSON 超过一万行；机柜细节由生成脚本重复展开，尚无组件定义、实例参数、版本或替换机制。
4. **状态层仍与演示逻辑耦合。** 服务端对所有可绑定对象生成温度、CPU 和功率，包括并不适用这些指标的机柜、摄像头和灭火器。
5. **没有真实数据语义。** 没有数据质量、来源点位、断连、过期、未知、单位校验和最后可信时间。
6. **缺少校准与变更模型。** executor 重建整个托管 Collection；没有把 AI 建议、人工确认值和后续变更分层保存。
7. **构建并非完全从源输入开始。** Web 构建依赖已经存在的 `build/server-room-l4.blend`，没有在同一脚本中从语义 JSON 重建该文件。
8. **自动化覆盖较薄。** 目前 3 个 compiler 单元测试通过，Web 构建通过，但没有 executor、GLB extras、API、WebSocket、点选映射或端到端回归测试。

## 下一轮验证：只做一个真实房间

下一轮不要继续增加合成场景细节。应冻结演示，选择一个可人工核对的小房间，完成以下实验：

### 输入

- 一张布局图或手绘平面图；
- 8–20 张按拍摄规范获取、有重叠区域的现场照片；
- 至少一个已知长度作为尺度锚点；
- 一份含稳定资产 ID 的设备表；
- 一个只读、已授权的真实状态数据源。

### 系统必须输出

- 每个识别对象的类别、位置、尺寸、证据图片和置信度；
- 无法判断、被遮挡和相互冲突的待确认项；
- AI 建议值与人工确认值分开保存；
- 资产匹配建议及确认记录；
- 数据源、点位、单位、更新时间和 stale/unknown 状态；
- 一次设备移动或新增后的差异，而不是全量覆盖。

### 同一验收标准下记录

- 纯人工总工时与 AI 辅助总工时；
- AI/计算费用和资料准备时间；
- 对象遗漏、误识别、位置/尺寸误差；
- 资产误绑定数量及修正时间；
- 数据点位一致率；
- 一次现场变更的维护工时；
- 第二个房间的组件复用率。

## 继续、转向或停止的门槛

以下阈值应在实验开始前冻结，避免事后挑选有利口径。建议第一轮采用：

- 必要资产覆盖率 ≥ 95%；
- 资产绑定准确率经人工验收达到 100%；
- 已知尺寸对象的位置/尺寸误差满足事先约定的业务容差；
- 所有无依据的推测明确标成待确认，不伪装成测量值；
- 真实遥测断开后能在规定时间进入 stale/unknown；
- AI 辅助总工时比同范围人工基线至少下降 30%；
- 完成一次变更时不丢失已经确认的人工校准；
- 第二房间确实复用了组件和流程，而不是再次由 ChatGPT 从零编程。

决策规则：

- **继续产品化：** 同范围下总工时显著下降，错误可发现、可纠正，第二现场仍能复用。
- **转为 AI 辅助交付服务：** 自动化不足，但少量专家借助工具能稳定、盈利地交付。
- **停止平台方向：** 主要成果仍是一场一代码，校准和真实数据接入抵消了建模节省，第二现场必须从零开始。

## 当前最准确的项目定位

在完成真实现场实验之前，建议对外使用下面的描述：

> 一个验证“空间语义 → Blender → Web 交互与状态展示”技术管线的开源原型。当前场景和遥测为合成数据；照片理解、真实资产绑定、持续更新及降本效果尚待真实现场验证。

这不是降低项目价值，而是把已经证明的部分和仍需证明的部分分开。只有这种边界清楚的项目，后续实验结果才可信。
